{
    "dependencies": {
        "@types/express": "^4.17.21",
        "@types/morgan": "^1.9.9",
        "express": "^4.18.2",
        "mongoose": "^8.0.3",
        "morgan": "^1.10.0"
    },
    "scripts": {
        "dev": "nodemon src/server.ts",
        "build": "pnpm clean && tsc --build",
        "clean": "rimraf dist",
        "start": "node dist/server.js"
    },
    "devDependencies": {
        "nodemon": "^3.0.2",
        "rimraf": "^5.0.5",
        "ts-node": "^10.9.2"
    }
}
